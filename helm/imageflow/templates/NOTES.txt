ImageFlow API deployed to {{ .Release.Namespace }}.

Get the application URL via port-forward:
  kubectl port-forward svc/{{ include "imageflow.fullname" . }} 8000:8000
Then: curl http://127.0.0.1:8000/health

Watch the rollout:
  kubectl rollout status deployment/{{ include "imageflow.fullname" . }}
