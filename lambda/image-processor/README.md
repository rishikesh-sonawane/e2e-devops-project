# lambda/image-processor

Image processing Lambda for ImageFlow: thumbnail generation + metadata extraction (Pillow, image-backed via Floci ECR).

See docs/architecture.md §3.2 (processing flow) and ADR-06 (image-backed Lambda).
